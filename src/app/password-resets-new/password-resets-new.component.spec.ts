/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PasswordResetsNewComponent } from './password-resets-new.component';

describe('Component: PasswordResetsNew', () => {
  it('should create an instance', () => {
    let component = new PasswordResetsNewComponent();
    expect(component).toBeTruthy();
  });
});
