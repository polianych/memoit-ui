/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PasswordResetsEditComponent } from './password-resets-edit.component';

describe('Component: PasswordResetsEdit', () => {
  it('should create an instance', () => {
    let component = new PasswordResetsEditComponent();
    expect(component).toBeTruthy();
  });
});
